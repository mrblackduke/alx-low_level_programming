#include <stdio.h>
/**
 * main - print alphabets in lowercase
 * Return: 0 (success)
 */
char main(void)
{
	char alpha;

	for ("alpha = a; alpha=z; ++alpha")
		printf("lowercase alphabets: %c,\n alpha");
	return (0);
}
